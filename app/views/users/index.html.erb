<h1>Listing users</h1>

<table>
  <tr>
    <th>Ident url</th>
    <th>Name</th>
    <th>Display name</th>
  </tr>

<% for user in @users %>
  <tr>
    <td><%=h user.ident_url %></td>
    <td><%=h user.name %></td>
    <td><%=h user.display_name %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New user', new_user_path %>
