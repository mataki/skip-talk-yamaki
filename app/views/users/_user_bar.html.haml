- if logged_in?
  #user-bar-greeting==Logged in as #{current_user.login}
  #user-bar-action= link_to "Log out", logout_path, { :title => "Log out" }
- else
  #user-bar-greeting= link_to_login_with_IP 'Not logged in', :style => 'border: none;'
  #user-bar-action= link_to "Log in",  login_path,  { :title => "Log in" }

